<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="@dimen/default_padding">

    <TextView
        android:id="@+id/tv_received_head"
        android:text="@string/message_received"
        style="@style/tv_head_msg" />

    <TextView
        android:id="@+id/tv_received_body"
        android:layout_below="@+id/tv_received_head"
        android:text="@string/is_msg"
        style="@style/tv_body_msg" />

    <Button
        android:id="@+id/btn_received"
        android:text="@string/reply"
        style="@style/btn_main"
        android:onClick="onReply"/>

    <EditText
        android:id="@+id/edit_received"
        android:layout_toStartOf="@+id/btn_received"
        android:hint="@string/edit_main"
        style="@style/edit_main"
        tools:ignore="Autofill" />

</RelativeLayout>